<Window x:Class="Pawshot_Windows.AnnotationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="画像注釈" Height="600" Width="800" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Topmost="True" MinWidth="850">
    
    <Window.Resources>
        <!-- ツールボタンのスタイル -->
        <Style x:Key="ToolButtonStyle" TargetType="RadioButton">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="17">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#55FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- アクションボタンのスタイル -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="17">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 完了ボタンのスタイル -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- ツールバー用 -->
            <RowDefinition Height="*"/>    <!-- 画像用 -->
        </Grid.RowDefinitions>

        <!-- ツールバー (上部に配置、画像に被らないようにする) -->
        <Border Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,15,0,5" 
                Background="#EE222222" CornerRadius="20" Padding="15,5" Height="45">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="ToolSelect" Style="{StaticResource ToolButtonStyle}" ToolTip="選択/移動 (S)">
                    <Path Data="M4,2 L11,18 L13,13 L18,11 Z" Fill="White" Stretch="Uniform" Width="18" Height="18" Margin="2,0,0,0"/>
                </RadioButton>
                <RadioButton x:Name="ToolRect" Style="{StaticResource ToolButtonStyle}" IsChecked="True" ToolTip="枠線 (R)">
                    <Path Data="M2,2 H18 V18 H2 Z" Stroke="White" StrokeThickness="2" Width="20" Height="20" Stretch="Uniform"/>
                </RadioButton>
                <RadioButton x:Name="ToolCircle" Style="{StaticResource ToolButtonStyle}" ToolTip="円 (O)">
                    <Ellipse Width="16" Height="16" Stroke="White" StrokeThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </RadioButton>
                <RadioButton x:Name="ToolArrow" Style="{StaticResource ToolButtonStyle}" ToolTip="矢印 (A)">
                    <Path Data="M2,18 L18,2 M18,2 L10,2 M18,2 L18,10" Stroke="White" StrokeThickness="2" Width="20" Height="20" Stretch="Uniform"/>
                </RadioButton>
                <RadioButton x:Name="ToolText" Style="{StaticResource ToolButtonStyle}" ToolTip="テキスト (T)">
                    <TextBlock Text="T" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </RadioButton>
                <RadioButton x:Name="ToolBlur" Style="{StaticResource ToolButtonStyle}" ToolTip="ぼかし/モザイク (B)">
                    <Grid Width="20" Height="20">
                        <!-- モザイク風のアイコン (2x2タイル) -->
                        <Rectangle Fill="White" HorizontalAlignment="Left" VerticalAlignment="Top" Width="9" Height="9" Opacity="0.8"/>
                        <Rectangle Fill="White" HorizontalAlignment="Right" VerticalAlignment="Top" Width="9" Height="9" Opacity="0.4"/>
                        <Rectangle Fill="White" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="9" Height="9" Opacity="0.4"/>
                        <Rectangle Fill="White" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="9" Height="9" Opacity="0.8"/>
                    </Grid>
                </RadioButton>

                <Separator Width="1" Background="#55FFFFFF" Margin="10,8" />

                <!-- 線の太さ選択 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                    <Border x:Name="ThickThin" Width="10" Height="10" Background="White" Margin="3,0" CornerRadius="5" Cursor="Hand" MouseDown="Thickness_MouseDown" ToolTip="細い (2px)" BorderThickness="1" BorderBrush="#88FFFFFF"/>
                    <Border x:Name="ThickMedium" Width="16" Height="16" Background="White" Margin="3,0" CornerRadius="8" Cursor="Hand" MouseDown="Thickness_MouseDown" ToolTip="普通 (5px)" BorderThickness="2" BorderBrush="White"/>
                    <Border x:Name="ThickHeavy" Width="22" Height="22" Background="White" Margin="3,0" CornerRadius="11" Cursor="Hand" MouseDown="Thickness_MouseDown" ToolTip="太い (10px)" BorderThickness="1" BorderBrush="#88FFFFFF"/>
                </StackPanel>

                <Separator Width="1" Background="#55FFFFFF" Margin="10,8" />

                <!-- カラーパレット -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                    <Border x:Name="PaletteRed" Width="20" Height="20" Background="Red" Margin="3,0" CornerRadius="10" Cursor="Hand" MouseDown="Color_MouseDown" BorderThickness="2" BorderBrush="White"/>
                    <Border x:Name="PaletteBlue" Width="20" Height="20" Background="Blue" Margin="3,0" CornerRadius="10" Cursor="Hand" MouseDown="Color_MouseDown"/>
                    <Border x:Name="PaletteGreen" Width="20" Height="20" Background="#FF00FF00" Margin="3,0" CornerRadius="10" Cursor="Hand" MouseDown="Color_MouseDown"/>
                    <Border x:Name="PaletteYellow" Width="20" Height="20" Background="Yellow" Margin="3,0" CornerRadius="10" Cursor="Hand" MouseDown="Color_MouseDown"/>
                    <Border x:Name="PaletteWhite" Width="20" Height="20" Background="White" Margin="3,0" CornerRadius="10" Cursor="Hand" MouseDown="Color_MouseDown"/>
                    <Border x:Name="PaletteBlack" Width="20" Height="20" Background="Black" Margin="3,0" CornerRadius="10" Cursor="Hand" MouseDown="Color_MouseDown"/>
                </StackPanel>
                
                <Separator Width="1" Background="#55FFFFFF" Margin="10,8" />
                
                <Button Click="Undo_Click" Style="{StaticResource ActionButtonStyle}" ToolTip="元に戻す (Ctrl+Z)">
                    <TextBlock Text="↩" Foreground="White" FontSize="20" VerticalAlignment="Center"/>
                </Button>
                
                <Separator Width="1" Background="#55FFFFFF" Margin="10,8" />
                
                <Button Click="Cancel_Click" Style="{StaticResource ActionButtonStyle}" ToolTip="キャンセル (Esc)">
                    <TextBlock Text="×" Foreground="White" FontSize="22" VerticalAlignment="Center"/>
                </Button>
                <Button Click="Done_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="5,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="完了" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="Bold"/>
                        <TextBlock Text="✓" VerticalAlignment="Center" FontSize="16"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- 背景の影付きボーダー (Row 1) -->
        <Border Grid.Row="1" x:Name="RootBorder" Margin="20,0,20,20" Background="White" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
            </Border.Effect>
            
            <Grid x:Name="CanvasGrid" ClipToBounds="True">
                <!-- キャプチャ画像 -->
                <Image x:Name="SourceImage" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top" />
                
                <!-- 描画レイヤー -->
                <Canvas x:Name="DrawingCanvas" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top"
                        MouseDown="DrawingCanvas_MouseDown" MouseMove="DrawingCanvas_MouseMove" MouseUp="DrawingCanvas_MouseUp" />
                
                <!-- 選択枠レイヤー (最前面) -->
                <Canvas HorizontalAlignment="Left" VerticalAlignment="Top" IsHitTestVisible="False">
                    <Rectangle x:Name="SelectionAdorner" Stroke="RoyalBlue" StrokeDashArray="3 1" StrokeThickness="1.5" Visibility="Collapsed"/>
                </Canvas>
            </Grid>
        </Border>
    </Grid>
</Window>
